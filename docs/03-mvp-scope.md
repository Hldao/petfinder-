# 03 · MVP 范围与功能规格

> **本文目的**：把当前 v8 原型已经定型的 6 个模块（首页 / 详情 / 发布 / 聊天 / 公众号 / 隐私协议）的功能、交互、数据需求、合规要点完整沉淀，作为开发期的对齐基线。
>
> **对照原型**：`prototypes/app-prototype-v8.html`
>
> **更新时间**：2026-05-23

---

## 一、MVP 整体规划

### 目标场景

大理本地的寻宠 / 拾遇互助：

```
失主发布 → 路人 / 拾主看到 → 留线索 / 联系 → 沟通确认 → 归还成功 → 写感谢信
```

### MVP 必做模块（已在 v8 原型中实现）

| # | 模块 | 屏幕 | 状态 |
|---|------|------|------|
| 1 | 信息流（首页 + 搜索）| feed / search | ✅ |
| 2 | 详情页（寻宠 + 招领）| detail-lost / detail-found | ✅ |
| 3 | 发布表单（双场景）| publish | ✅ |
| 4 | 站内聊天 | messages / chat | ✅ |
| 5 | 公众号关联 | mp-modal | ✅ |
| 6 | 启动隐私协议 | privacy-modal | ✅ |

### MVP 必做模块（并行开发中）

| # | 模块 | 屏幕 | 负责人 |
|---|------|------|--------|
| 7 | 地图视图 + 选点 | map | 小伙伴 |

### MVP 必做模块（待做）

| # | 模块 | 屏幕 | 备注 |
|---|------|------|------|
| 8 | 个人中心 + 善行值 | profile / points-history / my-posts | v8 已有基础，需细化 |
| 9 | 归还流程 | contact-guide / return-checklist / return-success | v8 已有基础 |
| 10 | 感谢信 | thanks-edit | v8 已有基础 |
| 11 | 争议申诉 | dispute | v8 已有基础 |

### V2 再做（不在 MVP 内）

- ❌ 视频通话 / 语音通话（需资质）
- ❌ 真正的群聊（合规复杂）
- ❌ 跨城市（先打透大理）
- ❌ AI 智能匹配（需训练数据）
- ❌ 徽章详情页
- ❌ 感谢信合集 / 月报

### 永远不做（合规红线）

- 🚫 充值 / 提现 / 积分商城
- 🚫 悬赏金 / 感谢金
- 🚫 拉新得积分（分销）
- 🚫 引导加微信 / 留电话

---

## 二、模块详细规格

### 模块 1 · 信息流（首页 + 搜索）

**屏幕**：`feed` + `search`

#### 1.1 首页 feed 功能清单

| 功能 | 描述 |
|------|------|
| 顶部导航 | 城市标签（**固定大理**，无下拉）/ 搜索栏 / 通知按钮 |
| 筛选 chip（4 选 1）| 全部 / 寻宠 / 招领 / 临时照料 |
| 排序 Action Sheet | 最新发布 / 距离最近 |
| 卡片列表 | 单排 + 数据驱动渲染（FEED_DATA → 14 张 mock）|
| 卡片底部统一规则 | `💬 留言数 · 👀 浏览量 · ⤴ 转发` |
| 转发 Modal | 准朋友圈分享卡（含品牌 / 大图 / 信息 / mock 二维码）|
| 中央凸起 FAB | 4 个 tab 屏共用，点击进发布 |
| 底部 Tab | 首页 / 地图 / 消息 / 我的 |

#### 1.2 搜索屏 search 功能清单

| 功能 | 描述 |
|------|------|
| 输入框 | 真实输入 + 自动聚焦 + 清除按钮 |
| 默认状态 | 最近搜索（mock）+ 大理热门搜索（8 个本地地标 / 品种）|
| 输入中 | 实时搜索建议（关键词高亮 `<mark>`）|
| 结果状态 | 复用卡片样式 + 匹配数量 |
| 空状态 | 友好提示 + 引导发布 |

#### 1.3 数据模型

```
posts (帖子)
├── id
├── type: 'lost' | 'found' | 'care'
├── status: 'pending_review' | 'approved' | 'returned' | 'expired'
├── pet: { name, breed, gender, age, photos[] }
├── location: { city, district, address, lat, lng, geohash }
├── time: timestamp
├── description (公开)
├── private_features (仅失主可见，加密存储)
├── contact_method: 'im' | 'virtual_phone'
├── poster_id → users
├── stats: { views, comments, shares, clues }
└── created_at / updated_at
```

#### 1.4 接口约定（粗）

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/feed?city=&filter=&sort=&page=` | 卡片列表（分页）|
| GET | `/search?q=&city=` | 搜索 |
| POST | `/shares` | 记录分享行为（统计）|

#### 1.5 合规要点

- 仅 `status = approved` 的内容才出现在 feed
- 9 天前内容自动透明度降低（前端 `expired` class）
- 位置精度模糊到 500m（数据库存 geohash 加密）
- 转发卡片含 mock 二维码（真实环境用 `wx.showShareImageMenu`）

---

### 模块 2 · 详情页

**屏幕**：`detail-lost` + `detail-found`

#### 2.1 共有结构

| 区块 | 内容 |
|------|------|
| Header | ← 返回 / 标题 / ↗ 转发（接 `openShare`）|
| Hero 大图 | 状态徽章（左上）+ 时间标识（右上，"走失 6 小时" / "招领 1 小时"）|
| 名字 / 副标题 | 名字 + 品种性别年龄等关键标签 |
| 信息网格 | 中文标签（走失地点 / 走失时间，或 发现地点 / 发现时间）|
| 描述卡 | 失主 / 拾主原话 |
| **发布者信息卡** | 头像 + 昵称 + 守护者徽章 + ✓ 实名 + 善行值 + 大理古城 + 历史归还次数 + **举报按钮** |
| 提示卡 | 黄色（私有特征提示）/ 绿色（沟通建议提示）|
| 底部 action-bar | 双按钮 |

#### 2.2 detail-lost 专属

| 区块 | 内容 |
|------|------|
| 留线索快捷入口 | 橘色渐变卡，点击跳 chat 留模板 |
| 已有线索区 | 线索列表（含 ✓ 有用 标记）|
| 底部主按钮 | `📌 我看到 ta 了`（涵盖"看到过"和"现在收着"两种场景）|

#### 2.3 detail-found 专属

| 区块 | 内容 |
|------|------|
| **目前状况区** | 目前位置 / 可照料天数（含倒计时）/ 健康状况 |
| 底部主按钮 | `🏠 这是我家的猫` → contact-guide |

#### 2.4 举报功能

5 种举报原因（沿用 Action Sheet 模式）：

- ⚠ 虚假信息
- 🔁 重复发布
- 🚫 违规内容（含微信号 / 电话 / 二维码）
- 💸 悬赏金 / 索要财物
- ⋯ 其他

#### 2.5 接口约定

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/posts/:id` | 详情 |
| POST | `/posts/:id/report` | 举报 |
| POST | `/posts/:id/clues` | 提交线索 |
| GET | `/posts/:id/clues` | 线索列表 |

#### 2.6 合规要点

- 私有特征**仅失主自己能看**（API 鉴权）
- 编辑后需**重新进入审核队列**（feed 上消失）
- 举报留存 ≥ 6 个月（电信条例）

---

### 模块 3 · 发布表单

**屏幕**：`publish`

#### 3.1 模式切换

```
seg: [🥺 我丢了宠物] [🤗 我捡到了]
              ↓ 切换
共用字段 + 各自专属字段
```

#### 3.2 字段对照

| 字段 | 我丢了（lost）| 我捡到了（found）|
|------|---------------|------------------|
| 照片 | ✅ ≥ 1 张 | ✅ ≥ 1 张 |
| 宠物类型 | ✅ 单选 | ✅ 单选 |
| 名字 | ✅ | ❌（捡到的不知道）|
| 品种 / 外观 | ✅ + 联想 chips | ✅ + 联想 chips |
| 性别 / 年龄 | ✅ | ✅（估计值）|
| 地点（**走失** / **发现**）| ✅ Action Sheet 选 | ✅ Action Sheet 选 |
| 详细地址 | 选填 | 选填 |
| 时间（**走失** / **发现**）| ✅ Action Sheet 选 | ✅ Action Sheet 选 |
| **目前位置** | ❌ | ✅ |
| **可临时照料天数** | ❌ | ✅ Action Sheet 选 |
| **健康状况** | ❌ | 选填 |
| 公开描述 | ✅ ≥ 10 字 | ✅ ≥ 10 字 |
| **私有特征**（防冒领）| ✅ + 4 个示例 chip | ❌（路人没独家信息）|
| 联系方式 | ✅ radio | ✅ radio |

#### 3.3 联想数据

**品种 chips（按宠物类型联动）**：

| 类型 | chips |
|------|-------|
| 猫 | 英短 / 美短 / 橘猫 / 布偶 / 田园猫 / 加菲 |
| 狗 | 金毛 / 柯基 / 比熊 / 泰迪 / 中华田园犬 / 拉布拉多 |
| 其他 | 兔子 / 鸟 / 仓鼠 / 龙猫 |

**地点 chips（大理本地）**：

- 使用当前位置 / 古城北门 / 人民路三月街 / 下关泰安路 / 喜洲古镇 / 海东

**时间 chips**：

- 刚刚（5 分钟内）/ 1 小时前 / 今天上午 / 今天下午 / 昨天 / 自定义

**可照料天数**：

- 1 天 / 3 天 / 一周 / 长期（直到找到主人）

**私有特征示例 chip**：

- 💍 项圈装饰
- 🐾 独特斑纹
- 😺 行为习惯
- 🩹 隐藏伤疤

#### 3.4 校验规则（前端）

| 规则 | 文案 |
|------|------|
| 照片 ≥ 1 张 | "请至少上传 1 张照片，帮助大家识别" |
| 地点必选 | "请选择地点" |
| 时间必选 | "请选择时间" |
| 描述 ≥ 10 字 | "描述至少 10 字，详细描述能帮助匹配成功率提高 3 倍" |
| 照片 ≤ 9 张 | "最多上传 9 张照片" |

#### 3.5 接口约定

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/posts` | 创建（先 pending_review）|
| POST | `/posts/:id/photos` | 照片上传 |
| GET | `/locations/recommended?city=` | 推荐地点 |

#### 3.6 合规要点

- **发布须知前置告知**（顶部红色卡片）
- 提交后调用 `security.msgSecCheck`（文字）+ `security.imgSecCheck`（图片）
- 未通过审核**不入库 / 不出现在 feed**
- 手机号字段加密（AES-256）存储
- 联系方式默认推荐"仅站内消息"

---

### 模块 4 · 站内聊天

**屏幕**：`messages` + `chat`

#### 4.1 messages 列表

| 来源 | 视觉区分 | 示例 |
|------|----------|------|
| 公众号通知 | 薄荷绿头像 + 「服务号」chip + 置顶 | "📍 古城北门附近有人捡到一只蓝猫" |
| 系统通知 | 灰色"系"字头像 | "陈* 的线索已被标记为有用" |
| 1v1 对话 | 宠物照头像 + 关于 chip | "好的我现在过来，您能视频先确认一下吗？" |

#### 4.2 chat 屏

| 区块 | 内容 |
|------|------|
| Header | ← 返回 / 对方昵称 / ⋯ 更多 |
| 宠物 card（顶部）| 缩略图 + 名字 + 状态 chip，点击跳详情 |
| 消息流 | 我的（橘色右对齐）/ 对方（白色左对齐，含阴影）/ 时间分隔 |
| **审核 bar** | "🛡 消息经审核后送达 · 含微信号 / 电话 / 二维码将被过滤" |
| **Quick Reply 6 个** | 横向滚动 chips |
| 输入栏 | 📷 / 📍 / input / ↑ |

#### 4.3 Quick Reply 模板

按"路人联系失主"视角排列：

| 模板 | 填入内容 | 视觉 |
|------|----------|------|
| 📍 我看到了 | "📍 我在 " (留位置占位) | 橘色 primary |
| 📸 我拍到了 | 完整句 | 灰底 |
| 🏠 我收着 ta | 完整句 + 见面询问 | **薄荷绿 action**（强意图）|
| 🕐 约时间 | 约见面句 | 灰底 |
| ❓ 视频确认 | 防冒领关键问题 | 灰底 |
| 🙏 谢谢 | 基础礼貌 | 灰底 |

#### 4.4 关键词过滤（前端预筛）

正则匹配命中即拒：

```
微信 | wechat | wx\s*[:：] | qq | 加我 | 私聊 | 联系方式 | 1[3-9]\d{9}
```

拒绝文案：「⚠ 消息含敏感内容（如微信号/电话/引导平台外联系）· 请使用站内消息沟通」

#### 4.5 数据模型

```
chats (会话)
├── id
├── post_id → posts
├── participants: [user_a, user_b]
├── created_at
└── last_message_at

messages (单条消息)
├── id
├── chat_id → chats
├── sender_id → users
├── content
├── type: 'text' | 'image' | 'location' | 'system'
├── status: 'sending' | 'sent' | 'blocked' | 'reviewing'
├── created_at
└── deleted_at
```

#### 4.6 接口约定

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/chats` | 我的会话列表 |
| GET | `/chats/:id/messages?before=&limit=` | 消息历史 |
| POST | `/chats/:id/messages` | 发送消息（同步调 msgSecCheck）|
| WebSocket / 轮询 | `/chats/:id/subscribe` | 实时更新 |

#### 4.7 合规要点

- **每条消息**必过 `msgSecCheck`（前端关键词过滤只是预筛）
- 图片消息必过 `imgSecCheck`
- 消息留存 ≥ 6 个月
- 注销账号时**保留消息**（脱敏处理）满足取证要求
- 进入 chat 时自动请求订阅消息授权（用于"对方新消息"推送）

---

### 模块 5 · 公众号关联

**目的**：解决推送 / 留存 / 传播痛点

#### 5.1 公众号定位

- **类型**：服务号（不是订阅号）
- **认证**：300 元/年（个体工商户主体已可申请）
- **关键能力**：
  - 关联小程序（自定义菜单跳小程序）
  - 服务通知模板（不受群发 4 次/月 限制）
  - 跟好友消息同级显示

#### 5.2 服务通知模板（待申请）

| 场景 | 模板内容示例 |
|------|--------------|
| 附近匹配 | "📍 古城北门附近有人捡到一只蓝猫，是你家胖丁吗？" |
| 新线索 | "📌 陈* 在洋人街看到了胖丁，留下了线索" |
| 归还成功 | "🎉 胖丁顺利回家了，谢谢你！→ 写感谢信" |
| 申诉处理 | "⚖ 你的申诉已处理 → 查看详情" |

#### 5.3 在小程序内的接入点

| 触点 | 时机 | 效果 |
|------|------|------|
| 启动 Modal | 用户首次同意隐私协议后自动弹 | 第一次进 app 建立公众号认知 |
| 个人中心菜单 | 「📣 关注公众号 · 推送更及时」| 主动想关注时找得到 |
| 消息列表置顶 | 公众号通知显示在列表顶部 | 让用户感受真实推送 |

#### 5.4 真实落地步骤

```
1. 注册公众号（1 周）
2. 服务号认证（300 元/年 + 1-3 天审核）
3. 关联小程序（公众号后台操作）
4. 设计自定义菜单（3 个一级：发布寻宠 / 看附近 / 我的）
5. 申请服务号模板消息（每个推送场景 1 个模板，1-2 周）
6. 持续运营：每周「大理回家了」周报 + 寻宠指南
```

---

### 模块 6 · 启动隐私协议

**屏幕**：`privacy-modal`

#### 6.1 4 个收集项明示

| 项 | 用途 | 处理方式 |
|----|------|----------|
| 📍 位置 | 发布走失/招领、附近匹配 | **精度模糊至 500m**（geohash 加密）|
| 📷 照片 | 宠物识别匹配 | **经平台审核** |
| 📞 手机号 | 联系（虚拟号转接）| **加密存储**，仅虚拟号供他人联系 |
| 💬 聊天内容 | 双方沟通 | **经审核送达，留存 6 个月** 满足监管 |

#### 6.2 流程

```
首次启动
  ↓
显示隐私 Modal（覆盖在首页上，不可绕过）
  ↓
用户点 [同意并继续]
  ↓
关闭隐私 Modal
  ↓ 0.25s 后自动
弹出 公众号关注 Modal（首次）
  ↓
用户可选择关注或稍后
```

#### 6.3 合规要点

- 必须在管理后台**填写「用户隐私保护指引」**
- 收集任何敏感信息前必须调 `requirePrivacyAuthorize`
- 提供「**注销账号**」入口（个人中心）
- 注销后**30 天宽限期**，期满彻底删除个人数据（聊天内容脱敏保留）

---

## 三、跨模块依赖

### 内容审核（贯穿所有 UGC 入口）

| 入口 | API |
|------|-----|
| publish | `msgSecCheck` (描述、私有特征) + `imgSecCheck` (照片) |
| chat | `msgSecCheck` (每条) + `imgSecCheck` (图片消息) |
| thanks-edit | `msgSecCheck` (感谢信) + `imgSecCheck` (合影) |
| dispute | `msgSecCheck` (申诉描述) + `imgSecCheck` (证据照) |
| 留线索 | `msgSecCheck` (线索内容) |

建议封装统一云函数 `contentSafety` 调用三个 API（详见 `docs/05-risks.md` 跨屏视角 A）。

### 善行值发放（贯穿多个场景）

| 行为 | 善行值 | 触发点 |
|------|--------|--------|
| 提供线索（基础）| +2 | 失主在详情页标记"有用" |
| 关键线索（追加）| +3 | 失主额外标记"关键" |
| 临时照料（完成）| +5 | 归还成功后 |
| 成功归还 | +4 | return-success |
| 收到感谢信 | +1 | 被失主点名感谢时 |

详见 `docs/01-points-system.md`。

### 状态机（贯穿多个表）

| 表 | 状态机 |
|----|--------|
| posts | pending_review → approved → returned / expired |
| chats | active → closed (归还后) |
| disputes | pending → investigating → resolved / dismissed |

详见 `docs/02-state-machines.md`。

---

## 四、接口清单（汇总）

| 模块 | 方法 | 路径 |
|------|------|------|
| **Feed** | GET | `/feed?city=&filter=&sort=&page=` |
| Feed | GET | `/search?q=&city=` |
| Feed | POST | `/shares` |
| **Posts** | POST | `/posts` |
| Posts | GET | `/posts/:id` |
| Posts | PUT | `/posts/:id` (重审) |
| Posts | DELETE | `/posts/:id` |
| Posts | POST | `/posts/:id/photos` |
| Posts | POST | `/posts/:id/report` |
| Posts | POST | `/posts/:id/clues` |
| Posts | GET | `/posts/:id/clues` |
| **Chats** | GET | `/chats` |
| Chats | GET | `/chats/:id/messages` |
| Chats | POST | `/chats/:id/messages` |
| **Locations** | GET | `/locations/recommended?city=` |
| **Users** | POST | `/users/login` (微信 code 换 openid) |
| Users | GET | `/users/me` |
| Users | DELETE | `/users/me` (注销) |
| **Notifications** | POST | `/notifications/subscribe` (公众号模板授权) |

详细字段定义见 `docs/04-database.md`（待写）。

---

## 五、跟其他文档的关系

| 文档 | 内容 |
|------|------|
| [`00-overview.md`](./00-overview.md) | 项目背景 + 目标市场 |
| [`01-points-system.md`](./01-points-system.md) | 善行值规则细节 |
| [`02-state-machines.md`](./02-state-machines.md) | 三张核心表的状态机 |
| **[`05-risks.md`](./05-risks.md)** | **合规风险 + 15 屏逐屏标注** ★ |
| [`06-product-philosophy.md`](./06-product-philosophy.md) | 产品哲学（"平台不留痕"等） |
| `04-database.md` | 数据库详细字段（待写）|

---

## 六、开发优先级建议

### P0（MVP 上线必须）

1. ✅ 信息流（首页 + 搜索）
2. ✅ 详情页
3. ✅ 发布表单
4. ✅ 站内聊天
5. 🚧 地图（小伙伴在做）
6. 🚧 个人中心 + 善行值
7. 🚧 归还流程
8. 🚧 隐私协议 / 注销账号 / 客服

### P1（MVP 后期补）

- 感谢信
- 争议申诉
- 公众号关联（同步进行）

### P2（V2）

- 徽章详情 / 月报 / 救助站合作
- AI 匹配
- 群聊
